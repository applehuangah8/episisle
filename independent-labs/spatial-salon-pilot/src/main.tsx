import React from "react";
import ReactDOM from "react-dom/client";

import { SalonPilotApp } from "./ui/SalonPilotApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SalonPilotApp />
  </React.StrictMode>
);
